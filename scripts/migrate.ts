// Migration stub. In a real setup, you might call `prisma migrate deploy`.
async function main() {
  console.log('[migrate] placeholder - wire your migration tool here');
}

main().catch((e) => { console.error(e); process.exit(1); });

